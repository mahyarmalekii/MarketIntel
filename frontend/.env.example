# Leave blank in local dev — vite.config.ts proxies /api and /ws to localhost:2860
VITE_BACKEND_URL=

# Only needed if API_SECRET is set on the backend AND you want the frontend
# to skip the /api/v1/token call (micro-optimisation, not required)
VITE_API_TOKEN=
